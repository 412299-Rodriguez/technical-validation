# --- DataSource (local MySQL) ---
spring.datasource.url=jdbc:mysql://localhost:3306/ficticia_insurance?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root      # o el usuario que uses
spring.datasource.password=root      # cambiá esto si ya tenés otro

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

jwt.secret=Y2hhbmdlLXRoaXMtdG8tYS12ZXJ5LXN0cm9uZy1zZWNyZXQta2V5LXdpdGgtNTQtYnl0ZXM=
jwt.expiration-millis=3600000
app.cors.allowed-origins=http://localhost:4200

# --- Mail / password recovery ---
spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:1025}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:false}
spring.mail.properties.mail.smtp.starttls.required=${MAIL_SMTP_STARTTLS:false}
app.frontend.base-url=${APP_FRONTEND_BASE_URL:http://localhost:4200}
app.security.reset-token-minutes=${APP_RESET_TOKEN_MINUTES:60}
